<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/head.ejs') %>
<body>
    <%- include('./partials/nav.ejs') %>

      <h2>Home</h2>
      <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Est eveniet nemo quisquam aliquid assumenda, soluta dolorem suscipit, vel ex tempore mollitia, eum dolorum. Voluptas similique natus, autem inventore accusantium blanditiis doloribus temporibus beatae, officiis rem eos, cumque tempore molestiae quo.</p>

          <div class="all notes">
          
            <% if (notes.length > 0) { %>
              <% notes.forEach(note => { %>
              
                  <h3 class="title"><%= note.title %></h3>
                  <% const str = note.body; %>
                  <% if (str.length < 100) { %>
                    <p class="body"><%= note.body %></p>
                  <% } else { %>
                    <p><% note.body.substring(1, 20); %> ..<a href="/notes/<%= note._id %>">read more</a></p>
                  
                 <% } %>
                  
                </a>
              <% }) %>
            <% } else { %>
              <p>There are no blogs to display...</p>
            <% } %>

         



          </div>


              


      <%- include('./partials/footer.ejs') %>

</body>
 
<%- include('./partials/script.ejs') %>

</html>